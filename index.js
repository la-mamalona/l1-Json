function main(){
  /** 
    Create a JSON variable that contains:
      name attribute
      age attribute
      gender attribute
      colors array with at least 3 colors (strings)
      address attribute with street subattribute
      classes array with at least 2 objects with name attribute
  */
  let my_json = {};
  return my_json;
}

module.exports = main;
