function main(){
  /** 
    Create a JSON variable that contains:
      computer_brand attribute
      is_windows attribute
      materials array with at least 2 materials (strings)
      os_info attribute with kernel_version subattribute
      apps_installed array with at least 2 objects with name attribute
  */
  let my_json = {};
  return my_json;
}

module.exports = main;
